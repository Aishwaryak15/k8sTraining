kind: ReplicaSet
apiVersion: apps/v1
metadata:
  name: myreplicaset
spec:
  replicas: 2
  selector:
    matchExpressions:
      - {key: city, operator: In, values: [pune]}
      - {key: myName, operator: NotIn, values: [Aishwarya]}
  template:
    metadata:
      name: testpods
      labels:
        myname: Aishwarya
    spec:
      containers:
      - name: c00
        image: ubuntu
        command: ["/bin/bash", "-c", "while true; do echo Hello-aish; sleep 5 ; done"]
